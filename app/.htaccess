<IfModule mod_rewrite.c>
    RewriteEngine on
    RewriteBase / 
    RewriteRule    ^$    webroot/    [L]
    RewriteRule    (.*) webroot/$1    [L]
</IfModule>
<IfModule mod_rewrite.c>

	RewriteEngine on

	RewriteBase	/wtbone/app/

	RewriteRule	^$	webroot/	[L]

	RewriteRule	(.*) webroot/$1	[L]

</IfModule>